node_modules
dist
*.local
.DS_Store

# Persoenliche Referenzen (SRD-Markdown, PDFs, Notizen) — nur lokal, nicht ins Repo.
reference/

# img/ ist ein lokaler Arbeitsordner fuer Quell-/Referenzbilder.
# Nur die Quellbilder, aus denen die App-Assets abgeleitet sind, gehoeren ins Repo.
img/*
!img/logo-source.jpeg
!img/logo_new.jpeg
!img/background.jpeg
!img/heller-hintergrund.jpeg
!img/neue-maus.jpeg
!img/keine-maus.jpeg
!img/tisch-leer.jpeg

# Rechtstexte-Quellen (die fertigen Seiten liegen in public/)
imp/

# Einmal-Werkzeug zum Bereinigen der History (enthaelt PII im Klartext) — nach Gebrauch loeschen
/.scrub/
